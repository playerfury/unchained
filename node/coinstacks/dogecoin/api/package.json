{
  "name": "@shapeshiftoss/dogecoin-api",
  "version": "12.0.0",
  "license": "MIT",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn generate && tsc",
    "clean": "rm -rf dist node_modules src/routes.ts src/swagger.json",
    "generate": "tsoa spec-and-routes"
  },
  "dependencies": {
    "@shapeshiftoss/blockbook": "^12.0.0",
    "@shapeshiftoss/common-api": "^12.0.0",
    "bech32": "^2.0.0"
  }
}
